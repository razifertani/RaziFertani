name: RaziFertani
description: 58116113

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  get_it:
  flutter_bloc: ^0.21.0
  equatable: ^0.4.0
  dartz:
  data_connection_checker:
  bottom_bar_page_transition: ^1.0.3  
  smooth_star_rating: ^1.1.1
  image_stack: ^1.1.0
  google_fonts: ^1.1.2
  
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - Assets/Images/